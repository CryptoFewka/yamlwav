# Exact development/test dependency versions for Python 3.9–3.14.
# Install with: python -m pip install -r requirements-dev.txt -e ".[dev]"
# Keep pyproject.toml's dev extra flexible; update this file with Dependabot.

# Direct dependencies. pytest 9 requires Python 3.10+.
pytest==8.4.2
ruamel.yaml==0.19.1
PyYAML==6.0.3

# Transitive dependencies. iniconfig 2.2+ requires Python 3.10+.
iniconfig==2.1.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
tomli==2.4.1 ; python_version < "3.11"
exceptiongroup==1.3.1 ; python_version < "3.11"
typing-extensions==4.16.0 ; python_version < "3.11"
colorama==0.4.6 ; sys_platform == "win32"
